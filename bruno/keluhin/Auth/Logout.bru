meta {
  name: Logout
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/logout
  body: none
  auth: inherit
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("token", "");
  }
}
